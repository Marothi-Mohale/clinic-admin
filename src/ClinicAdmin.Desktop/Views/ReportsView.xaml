<UserControl x:Class="ClinicAdmin.Desktop.Views.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:ClinicAdmin.Desktop.Controls"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="16" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="16" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Style="{StaticResource CardBorderStyle}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="160" />
                    <ColumnDefinition Width="20" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="160" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock VerticalAlignment="Center" FontWeight="SemiBold" Text="From" />
                <DatePicker Grid.Column="1"
                            Style="{StaticResource DatePickerInputStyle}"
                            SelectedDate="{Binding FromDate, Mode=TwoWay}"
                            />
                <TextBlock Grid.Column="3" VerticalAlignment="Center" FontWeight="SemiBold" Text="To" />
                <DatePicker Grid.Column="4"
                            Style="{StaticResource DatePickerInputStyle}"
                            SelectedDate="{Binding ToDate, Mode=TwoWay}"
                            />

                <Button Grid.Column="6"
                        Width="120"
                        HorizontalAlignment="Right"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Command="{Binding LoadReportCommand}"
                        Content="Load report" />
                <Button Grid.Column="7"
                        Width="120"
                        Margin="12,0,0,0"
                        Style="{StaticResource SecondaryButtonStyle}"
                        Command="{Binding ExportCsvCommand}"
                        Content="Export CSV" />
                <Button Grid.Column="8"
                        Width="90"
                        Margin="12,0,0,0"
                        Style="{StaticResource SecondaryButtonStyle}"
                        Command="{Binding ResetFiltersCommand}"
                        Content="Reset" />
            </Grid>
        </Border>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="2*" />
            </Grid.ColumnDefinitions>

            <controls:MetricCard Title="Total registrations" Value="{Binding TotalRegistrations}" />
            <controls:MetricCard Grid.Column="2" Title="Total visits" Value="{Binding TotalVisits}" />

            <Border Grid.Column="4" Style="{StaticResource CardBorderStyle}">
                <TextBlock Style="{StaticResource SubtleTextStyle}" Text="{Binding StatusMessage}" VerticalAlignment="Center" />
            </Border>
        </Grid>

        <Grid Grid.Row="4">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="16" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.1*" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="1.1*" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="1.3*" />
            </Grid.ColumnDefinitions>

            <Border Style="{StaticResource CardBorderStyle}" Padding="18">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="12" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBlock Style="{StaticResource SectionTitleTextStyle}" Text="Daily registrations" />
                    <DataGrid Grid.Row="2" ItemsSource="{Binding DailyRegistrations}" Style="{StaticResource FlatDataGridStyle}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Date" Binding="{Binding Date}" Width="*" />
                            <DataGridTextColumn Header="Registrations" Binding="{Binding RegistrationCount}" Width="140" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource CardBorderStyle}" Padding="18">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="12" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBlock Style="{StaticResource SectionTitleTextStyle}" Text="Visits per day" />
                    <DataGrid Grid.Row="2" ItemsSource="{Binding DailyVisits}" Style="{StaticResource FlatDataGridStyle}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Date" Binding="{Binding Date}" Width="*" />
                            <DataGridTextColumn Header="Visits" Binding="{Binding VisitCount}" Width="120" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

            <Border Grid.Column="4" Style="{StaticResource CardBorderStyle}" Padding="18">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="12" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBlock Style="{StaticResource SectionTitleTextStyle}" Text="Common reasons for visit" />
                    <DataGrid Grid.Row="2" ItemsSource="{Binding CommonReasons}" Style="{StaticResource FlatDataGridStyle}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Reason" Binding="{Binding ReasonForVisit}" Width="*" />
                            <DataGridTextColumn Header="Visits" Binding="{Binding VisitCount}" Width="100" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

            <Border Grid.Row="2" Style="{StaticResource CardBorderStyle}" Padding="18">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="12" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBlock Style="{StaticResource SectionTitleTextStyle}" Text="Staff activity summary" />
                    <DataGrid Grid.Row="2" ItemsSource="{Binding StaffActivity}" Style="{StaticResource FlatDataGridStyle}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Staff" Binding="{Binding DisplayName}" Width="*" />
                            <DataGridTextColumn Header="Role" Binding="{Binding Role}" Width="110" />
                            <DataGridTextColumn Header="Logins" Binding="{Binding SuccessfulLogins}" Width="70" />
                            <DataGridTextColumn Header="Failed" Binding="{Binding FailedLogins}" Width="70" />
                            <DataGridTextColumn Header="Registrations" Binding="{Binding PatientRegistrations}" Width="100" />
                            <DataGridTextColumn Header="Visits" Binding="{Binding VisitsRegistered}" Width="70" />
                            <DataGridTextColumn Header="Updates" Binding="{Binding VisitsUpdated}" Width="70" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

            <Border Grid.Row="2" Grid.Column="2" Grid.ColumnSpan="3" Style="{StaticResource CardBorderStyle}" Padding="18">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="12" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBlock Style="{StaticResource SectionTitleTextStyle}" Text="Patient visit history summary" />
                    <DataGrid Grid.Row="2" ItemsSource="{Binding PatientVisitHistory}" Style="{StaticResource FlatDataGridStyle}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Patient no" Binding="{Binding PatientNumber}" Width="110" />
                            <DataGridTextColumn Header="Patient" Binding="{Binding DisplayName}" Width="*" />
                            <DataGridTextColumn Header="Visits" Binding="{Binding VisitCount}" Width="70" />
                            <DataGridTextColumn Header="Last visit (UTC)" Binding="{Binding LastVisitAtUtc}" Width="160" />
                            <DataGridTextColumn Header="Last reason" Binding="{Binding LastReasonForVisit}" Width="*" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
